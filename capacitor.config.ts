import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ModelAI.app',
  appName: 'ModelAI: Predictive Sports Analytics',
  webDir: 'dist'
};

export default config;
